# Root .env — read by Docker Compose only.
# Do NOT put API keys or app secrets here.
# Copy to .env and fill in values.

# PostgreSQL password used by docker-compose.yml
POSTGRES_PASSWORD=trading_dev
